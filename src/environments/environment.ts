// Entorno para desarrollo local (sin proxy)
export const environment = {
  production: false,
  name: 'local',
  apiUrl: 'http://localhost:3000', // URL directa al backend sin /api
  useProxy: false
};
