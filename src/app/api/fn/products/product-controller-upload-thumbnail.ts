/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProductResponseDto } from '../../models/product-response-dto';

export interface ProductControllerUploadThumbnail$Params {
  id: number;
      body: {
'file'?: Blob;
}
}

export function productControllerUploadThumbnail(http: HttpClient, rootUrl: string, params: ProductControllerUploadThumbnail$Params, context?: HttpContext): Observable<StrictHttpResponse<ProductResponseDto>> {
  const rb = new RequestBuilder(rootUrl, productControllerUploadThumbnail.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProductResponseDto>;
    })
  );
}

productControllerUploadThumbnail.PATH = '/products/{id}/upload-thumbnail';
