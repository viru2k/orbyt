/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RoomResponseDto } from '../../models/room-response-dto';
import { UpdateRoomDto } from '../../models/update-room-dto';

export interface RoomControllerUpdate$Params {
  id: number;
      body: UpdateRoomDto
}

export function roomControllerUpdate(http: HttpClient, rootUrl: string, params: RoomControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<RoomResponseDto>> {
  const rb = new RequestBuilder(rootUrl, roomControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoomResponseDto>;
    })
  );
}

roomControllerUpdate.PATH = '/rooms/{id}';
