/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RoomResponseDto } from '../../models/room-response-dto';

export interface RoomControllerToggleStatus$Params {
  id: number;
}

export function roomControllerToggleStatus(http: HttpClient, rootUrl: string, params: RoomControllerToggleStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<RoomResponseDto>> {
  const rb = new RequestBuilder(rootUrl, roomControllerToggleStatus.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoomResponseDto>;
    })
  );
}

roomControllerToggleStatus.PATH = '/rooms/{id}/toggle-status';
