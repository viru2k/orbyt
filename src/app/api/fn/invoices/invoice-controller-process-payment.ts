/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { InvoiceResponseDto } from '../../models/invoice-response-dto';
import { ProcessPaymentDto } from '../../models/process-payment-dto';

export interface InvoiceControllerProcessPayment$Params {
  id: number;
      body: ProcessPaymentDto
}

export function invoiceControllerProcessPayment(http: HttpClient, rootUrl: string, params: InvoiceControllerProcessPayment$Params, context?: HttpContext): Observable<StrictHttpResponse<InvoiceResponseDto>> {
  const rb = new RequestBuilder(rootUrl, invoiceControllerProcessPayment.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InvoiceResponseDto>;
    })
  );
}

invoiceControllerProcessPayment.PATH = '/api/invoices/{id}/pay';
