/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { InvoiceResponseDto } from '../../models/invoice-response-dto';
import { UpdateInvoiceDto } from '../../models/update-invoice-dto';

export interface InvoiceControllerUpdate$Params {
  id: number;
      body: UpdateInvoiceDto
}

export function invoiceControllerUpdate(http: HttpClient, rootUrl: string, params: InvoiceControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<InvoiceResponseDto>> {
  const rb = new RequestBuilder(rootUrl, invoiceControllerUpdate.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InvoiceResponseDto>;
    })
  );
}

invoiceControllerUpdate.PATH = '/invoices/{id}';
