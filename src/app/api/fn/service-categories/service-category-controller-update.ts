/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ServiceCategoryResponseDto } from '../../models/service-category-response-dto';
import { UpdateServiceCategoryDto } from '../../models/update-service-category-dto';

export interface ServiceCategoryControllerUpdate$Params {
  id: string;
      body: UpdateServiceCategoryDto
}

export function serviceCategoryControllerUpdate(http: HttpClient, rootUrl: string, params: ServiceCategoryControllerUpdate$Params, context?: HttpContext): Observable<StrictHttpResponse<ServiceCategoryResponseDto>> {
  const rb = new RequestBuilder(rootUrl, serviceCategoryControllerUpdate.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ServiceCategoryResponseDto>;
    })
  );
}

serviceCategoryControllerUpdate.PATH = '/categories/{id}';
