/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProductInventoryConfigDto } from '../../models/product-inventory-config-dto';

export interface InventoryControllerGetInventoryConfig$Params {

/**
 * ID del producto
 */
  id: number;
}

export function inventoryControllerGetInventoryConfig(http: HttpClient, rootUrl: string, params: InventoryControllerGetInventoryConfig$Params, context?: HttpContext): Observable<StrictHttpResponse<ProductInventoryConfigDto>> {
  const rb = new RequestBuilder(rootUrl, inventoryControllerGetInventoryConfig.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProductInventoryConfigDto>;
    })
  );
}

inventoryControllerGetInventoryConfig.PATH = '/inventory/products/{id}/inventory-config';
