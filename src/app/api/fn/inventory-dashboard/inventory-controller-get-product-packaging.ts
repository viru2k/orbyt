/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProductPackagingDto } from '../../models/product-packaging-dto';

export interface InventoryControllerGetProductPackaging$Params {

/**
 * ID del producto
 */
  id: number;
}

export function inventoryControllerGetProductPackaging(http: HttpClient, rootUrl: string, params: InventoryControllerGetProductPackaging$Params, context?: HttpContext): Observable<StrictHttpResponse<ProductPackagingDto>> {
  const rb = new RequestBuilder(rootUrl, inventoryControllerGetProductPackaging.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProductPackagingDto>;
    })
  );
}

inventoryControllerGetProductPackaging.PATH = '/inventory/products/{id}/packaging';
