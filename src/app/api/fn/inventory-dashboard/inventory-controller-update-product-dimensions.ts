/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProductDimensionsDto } from '../../models/product-dimensions-dto';
import { UpdateProductDimensionsDto } from '../../models/update-product-dimensions-dto';

export interface InventoryControllerUpdateProductDimensions$Params {

/**
 * ID del producto
 */
  id: number;
      body: UpdateProductDimensionsDto
}

export function inventoryControllerUpdateProductDimensions(http: HttpClient, rootUrl: string, params: InventoryControllerUpdateProductDimensions$Params, context?: HttpContext): Observable<StrictHttpResponse<ProductDimensionsDto>> {
  const rb = new RequestBuilder(rootUrl, inventoryControllerUpdateProductDimensions.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProductDimensionsDto>;
    })
  );
}

inventoryControllerUpdateProductDimensions.PATH = '/inventory/products/{id}/dimensions';
