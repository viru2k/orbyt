/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TopMoversDto } from '../../models/top-movers-dto';

export interface InventoryControllerGetTopMovers$Params {
  limit?: number;
  days?: number;
}

export function inventoryControllerGetTopMovers(http: HttpClient, rootUrl: string, params?: InventoryControllerGetTopMovers$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TopMoversDto>>> {
  const rb = new RequestBuilder(rootUrl, inventoryControllerGetTopMovers.PATH, 'get');
  if (params) {
    rb.query('limit', params.limit, {});
    rb.query('days', params.days, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TopMoversDto>>;
    })
  );
}

inventoryControllerGetTopMovers.PATH = '/inventory/dashboard/top-movers';
