/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProductInventoryConfigDto } from '../../models/product-inventory-config-dto';
import { UpdateProductInventoryConfigDto } from '../../models/update-product-inventory-config-dto';

export interface InventoryControllerUpdateInventoryConfig$Params {

/**
 * ID del producto
 */
  id: number;
      body: UpdateProductInventoryConfigDto
}

export function inventoryControllerUpdateInventoryConfig(http: HttpClient, rootUrl: string, params: InventoryControllerUpdateInventoryConfig$Params, context?: HttpContext): Observable<StrictHttpResponse<ProductInventoryConfigDto>> {
  const rb = new RequestBuilder(rootUrl, inventoryControllerUpdateInventoryConfig.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProductInventoryConfigDto>;
    })
  );
}

inventoryControllerUpdateInventoryConfig.PATH = '/inventory/products/{id}/inventory-config';
