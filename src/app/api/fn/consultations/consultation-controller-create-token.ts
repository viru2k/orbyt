/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ConsultationTokenResponseDto } from '../../models/consultation-token-response-dto';
import { CreateConsultationTokenDto } from '../../models/create-consultation-token-dto';

export interface ConsultationControllerCreateToken$Params {
  id: number;
      body: CreateConsultationTokenDto
}

export function consultationControllerCreateToken(http: HttpClient, rootUrl: string, params: ConsultationControllerCreateToken$Params, context?: HttpContext): Observable<StrictHttpResponse<ConsultationTokenResponseDto>> {
  const rb = new RequestBuilder(rootUrl, consultationControllerCreateToken.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ConsultationTokenResponseDto>;
    })
  );
}

consultationControllerCreateToken.PATH = '/consultations/{id}/tokens';
