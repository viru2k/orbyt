/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateStockMovementDto } from '../../models/create-stock-movement-dto';
import { StockMovementResponseDto } from '../../models/stock-movement-response-dto';

export interface StockControllerCreate$Params {
      body: CreateStockMovementDto
}

export function stockControllerCreate(http: HttpClient, rootUrl: string, params: StockControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<StockMovementResponseDto>> {
  const rb = new RequestBuilder(rootUrl, stockControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StockMovementResponseDto>;
    })
  );
}

stockControllerCreate.PATH = '/api/stock';
