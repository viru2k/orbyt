/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { StockMovementResponseDto } from '../../models/stock-movement-response-dto';

export interface StockControllerGetMovements$Params {
  productId: number;
}

export function stockControllerGetMovements(http: HttpClient, rootUrl: string, params: StockControllerGetMovements$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<StockMovementResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, stockControllerGetMovements.PATH, 'get');
  if (params) {
    rb.path('productId', params.productId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<StockMovementResponseDto>>;
    })
  );
}

stockControllerGetMovements.PATH = '/api/stock/{productId}';
