/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface AgendaControllerGetDayOverrides$Params {

/**
 * ID del profesional (admin)
 */
  professionalId?: number;

/**
 * Fecha de inicio (YYYY-MM-DD)
 */
  from?: string;

/**
 * Fecha de fin (YYYY-MM-DD)
 */
  to?: string;
}

export function agendaControllerGetDayOverrides(http: HttpClient, rootUrl: string, params?: AgendaControllerGetDayOverrides$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, agendaControllerGetDayOverrides.PATH, 'get');
  if (params) {
    rb.query('professionalId', params.professionalId, {});
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

agendaControllerGetDayOverrides.PATH = '/api/agenda/day-overrides';
