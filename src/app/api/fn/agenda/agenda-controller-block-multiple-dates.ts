/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BlockDatesDto } from '../../models/block-dates-dto';

export interface AgendaControllerBlockMultipleDates$Params {

/**
 * ID del profesional (admin)
 */
  professionalId?: number;
      body: BlockDatesDto
}

export function agendaControllerBlockMultipleDates(http: HttpClient, rootUrl: string, params: AgendaControllerBlockMultipleDates$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, agendaControllerBlockMultipleDates.PATH, 'post');
  if (params) {
    rb.query('professionalId', params.professionalId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

agendaControllerBlockMultipleDates.PATH = '/api/agenda/block-dates';
