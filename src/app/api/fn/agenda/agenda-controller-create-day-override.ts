/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DayOverrideDto } from '../../models/day-override-dto';

export interface AgendaControllerCreateDayOverride$Params {

/**
 * ID del profesional (admin)
 */
  professionalId?: number;
      body: DayOverrideDto
}

export function agendaControllerCreateDayOverride(http: HttpClient, rootUrl: string, params: AgendaControllerCreateDayOverride$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, agendaControllerCreateDayOverride.PATH, 'post');
  if (params) {
    rb.query('professionalId', params.professionalId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

agendaControllerCreateDayOverride.PATH = '/agenda/day-override';
