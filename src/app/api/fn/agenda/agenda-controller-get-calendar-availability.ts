/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CalendarAvailabilityDto } from '../../models/calendar-availability-dto';

export interface AgendaControllerGetCalendarAvailability$Params {

/**
 * Año (ej: 2025)
 */
  year: number;

/**
 * Mes (1-12)
 */
  month: number;

/**
 * ID del profesional (admin)
 */
  professionalId?: number;
}

export function agendaControllerGetCalendarAvailability(http: HttpClient, rootUrl: string, params: AgendaControllerGetCalendarAvailability$Params, context?: HttpContext): Observable<StrictHttpResponse<CalendarAvailabilityDto>> {
  const rb = new RequestBuilder(rootUrl, agendaControllerGetCalendarAvailability.PATH, 'get');
  if (params) {
    rb.query('year', params.year, {});
    rb.query('month', params.month, {});
    rb.query('professionalId', params.professionalId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CalendarAvailabilityDto>;
    })
  );
}

agendaControllerGetCalendarAvailability.PATH = '/agenda/calendar-availability';
