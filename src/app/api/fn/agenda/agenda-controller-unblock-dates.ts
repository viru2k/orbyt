/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface AgendaControllerUnblockDates$Params {

/**
 * ID del profesional (admin)
 */
  professionalId?: number;

/**
 * Fechas separadas por coma (2024-12-25,2024-12-26)
 */
  dates: string;
}

export function agendaControllerUnblockDates(http: HttpClient, rootUrl: string, params: AgendaControllerUnblockDates$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, agendaControllerUnblockDates.PATH, 'delete');
  if (params) {
    rb.query('professionalId', params.professionalId, {});
    rb.query('dates', params.dates, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

agendaControllerUnblockDates.PATH = '/agenda/block-dates';
