/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AppointmentSummaryResponseDto } from '../../models/appointment-summary-response-dto';

export interface AgendaControllerGetSummary$Params {

/**
 * Fecha de inicio (YYYY-MM-DD)
 */
  from: string;

/**
 * Fecha de fin (YYYY-MM-DD)
 */
  to: string;

/**
 * IDs de los profesionales (admin)
 */
  professionalId?: Array<number>;
}

export function agendaControllerGetSummary(http: HttpClient, rootUrl: string, params: AgendaControllerGetSummary$Params, context?: HttpContext): Observable<StrictHttpResponse<AppointmentSummaryResponseDto>> {
  const rb = new RequestBuilder(rootUrl, agendaControllerGetSummary.PATH, 'get');
  if (params) {
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
    rb.query('professionalId', params.professionalId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AppointmentSummaryResponseDto>;
    })
  );
}

agendaControllerGetSummary.PATH = '/agenda/summary';
