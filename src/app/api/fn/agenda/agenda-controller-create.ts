/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AppointmentResponseDto } from '../../models/appointment-response-dto';
import { CreateAppointmentDto } from '../../models/create-appointment-dto';

export interface AgendaControllerCreate$Params {
      body: CreateAppointmentDto
}

export function agendaControllerCreate(http: HttpClient, rootUrl: string, params: AgendaControllerCreate$Params, context?: HttpContext): Observable<StrictHttpResponse<AppointmentResponseDto>> {
  const rb = new RequestBuilder(rootUrl, agendaControllerCreate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AppointmentResponseDto>;
    })
  );
}

agendaControllerCreate.PATH = '/api/agenda';
