/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AgendaConfigResponseDto } from '../../models/agenda-config-response-dto';

export interface AgendaControllerGetConfig$Params {

/**
 * ID del profesional (admin)
 */
  professionalId?: number;
}

export function agendaControllerGetConfig(http: HttpClient, rootUrl: string, params?: AgendaControllerGetConfig$Params, context?: HttpContext): Observable<StrictHttpResponse<AgendaConfigResponseDto>> {
  const rb = new RequestBuilder(rootUrl, agendaControllerGetConfig.PATH, 'get');
  if (params) {
    rb.query('professionalId', params.professionalId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AgendaConfigResponseDto>;
    })
  );
}

agendaControllerGetConfig.PATH = '/api/agenda/config';
