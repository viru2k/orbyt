/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ClientAppointmentsListDto } from '../../models/client-appointments-list-dto';

export interface ClientPortalControllerGetAppointments$Params {

/**
 * Página (default: 1)
 */
  page?: number;

/**
 * Elementos por página (default: 10)
 */
  limit?: number;
}

export function clientPortalControllerGetAppointments(http: HttpClient, rootUrl: string, params?: ClientPortalControllerGetAppointments$Params, context?: HttpContext): Observable<StrictHttpResponse<ClientAppointmentsListDto>> {
  const rb = new RequestBuilder(rootUrl, clientPortalControllerGetAppointments.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ClientAppointmentsListDto>;
    })
  );
}

clientPortalControllerGetAppointments.PATH = '/api/client/profile/appointments';
