/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FileUploadResponseDto } from '../../models/file-upload-response-dto';
import { UpdateFileEntityDto } from '../../models/update-file-entity-dto';

export interface UploadControllerUpdateFileEntity$Params {
  id: number;
      body: UpdateFileEntityDto
}

export function uploadControllerUpdateFileEntity(http: HttpClient, rootUrl: string, params: UploadControllerUpdateFileEntity$Params, context?: HttpContext): Observable<StrictHttpResponse<FileUploadResponseDto>> {
  const rb = new RequestBuilder(rootUrl, uploadControllerUpdateFileEntity.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FileUploadResponseDto>;
    })
  );
}

uploadControllerUpdateFileEntity.PATH = '/upload/{id}/entity';
