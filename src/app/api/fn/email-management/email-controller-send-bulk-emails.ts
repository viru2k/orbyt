/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BulkEmailDto } from '../../models/bulk-email-dto';
import { BulkEmailResponseDto } from '../../models/bulk-email-response-dto';

export interface EmailControllerSendBulkEmails$Params {
      body: BulkEmailDto
}

export function emailControllerSendBulkEmails(http: HttpClient, rootUrl: string, params: EmailControllerSendBulkEmails$Params, context?: HttpContext): Observable<StrictHttpResponse<BulkEmailResponseDto>> {
  const rb = new RequestBuilder(rootUrl, emailControllerSendBulkEmails.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BulkEmailResponseDto>;
    })
  );
}

emailControllerSendBulkEmails.PATH = '/email/send/bulk';
